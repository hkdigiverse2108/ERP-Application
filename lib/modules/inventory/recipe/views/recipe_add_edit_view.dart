import 'package:flutter/material.dart';

class RecipeAddEditView extends StatelessWidget {
  const RecipeAddEditView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('Add/Edit Recipe')));
  }
}
